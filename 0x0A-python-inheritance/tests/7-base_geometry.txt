This is the BaseGeometry module

This is an example text file

First import BaseGeometry from the 1-my_list module:

    >>> BaseGeometry = __import__("7-base_geometry").BaseGeometry

Let's test it:

    >>> geometry = BaseGeometry()

    checking integer integer_validator with an integer
    >>> print(geometry.integer_validator("side_length", 5))
    None

    checking with float
    >>> print(geometry.integer_validator("side_length", 5.5))
    Traceback (most recent call last):
        ...
    TypeError: name must be an integer
